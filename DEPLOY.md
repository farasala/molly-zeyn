# DEPLOY.md — этап 1

Этап 1 закрыт и проверен на живом адресе. Здесь — что где лежит и что осталось на потом.

---

## Инфраструктура

### Supabase

Проект **english-studio**, регион **eu-central-1 (Франкфурт)** — Алматы ходит в Европу заметно
быстрее, чем в Сидней, где был первый проект. Старый проект `farasala's Project` не тронут,
удалите его сами.

- `schema.sql` выполнен целиком: 7 таблиц, триггер `on_auth_user_created`, функция `teaches`,
  все RLS-политики. RLS включён на каждой таблице.
- Бакеты Storage: `audio` (публичный) и `homework` (приватный) с политиками.
- `handle_new_user` закрыта от вызова через REST (`revoke execute`).
- Подтверждение почты выключено: `mailer_autoconfirm: true`.

| Что | Значение |
|---|---|
| Project URL | `https://uwkosphzadpfonjvjanb.supabase.co` |
| Publishable key | `sb_publishable_3PB2N1WM8o17EYkD1aNybQ_UxjexNX_` |

Оба значения публичные по замыслу — они уходят в браузер, их прикрывает RLS.
`service_role key` не используется и никуда не прописан.

### GitHub → Vercel

| Что | Значение |
|---|---|
| Репозиторий | `https://github.com/farasala/molly-zeyn`, ветка `main` |
| Сайт | `https://english-studio-molly-9f55.vercel.app` |
| Регион сборки | `fra1` — рядом с базой |

Репозиторий связан с проектом Vercel. Проверено делом: пуш в `main` пересобрал сайт за 40
секунд. **Дальше просто `git push` — деплой сам.**

Переменные окружения на Vercel (Production, Preview, Development):

```
NEXT_PUBLIC_SUPABASE_URL       = https://uwkosphzadpfonjvjanb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY  = sb_publishable_3PB2N1WM8o17EYkD1aNybQ_UxjexNX_
```

`NEXT_PUBLIC_*` вшиваются в сборку — если менять их, нужен Redeploy.

Локально: скопируйте `.env.example` в `.env.local` и вставьте те же два значения.
`.env.local` в `.gitignore`.

---

## Этап 2 — контент

Живой курс: `/levels`, `/levels/elementary`, `/lessons/[id]` с пятью вкладками.

Проверено на боевом адресе: все 36 уроков открываются, 6 живых показывают все пять вкладок,
30 закрытых — панель «content in production», 78 аудиофайлов отдаются и реально играют.

**Аудио отдаётся приложением из `public/audio/el/`, а не из Storage.** Бакет `audio` создан
и публичен, но запись в него требует `service_role key`, которого у приложения быть не должно —
залить туда файлы автоматически отсюда нельзя. Когда захотите перенести: загрузите папку
в дашборде Supabase и пропишите переменную

```
NEXT_PUBLIC_AUDIO_BASE = https://uwkosphzadpfonjvjanb.supabase.co/storage/v1/object/public/audio/el
```

Больше ничего менять не нужно — `lib/audio.ts` читает только её.

---

## Проверка этапа 1 — пройдена

Всё гонялось на боевом адресе, не локально:

- экраны входа и регистрации открываются, Montserrat грузится, цвета из дизайн-системы;
- регистрация студента сразу пускает внутрь, без письма;
- строка в `profiles` создаётся с правильными именем и ролью;
- перезагрузка страницы — сессия держится;
- вошедший на `/login` уходит на `/dashboard`, вышедший на `/dashboard` — на `/login`;
- выход работает, повторный вход тем же паролем работает;
- неверный пароль даёт «Email or password is not right. Check both and try again.»,
  почта в поле сохраняется;
- сессия переживает пересборку сайта;
- аноним через REST с публичным ключом читает `profiles` и `activity_results` как `[]`,
  вызов `rpc/handle_new_user` даёт 404.

Осталось за вами формально: **зарегистрироваться именно с телефона** и увидеть студента
в Supabase на ноутбуке. Вёрстка на 375px проверена, специфичного для устройства в коде нет.

---

## Что осталось на потом

### Домен

Купить на hoster.kz только домен (хостинг не нужен), затем Vercel → **Settings** → **Domains** →
добавить `example.kz` и `www.example.kz`. Vercel покажет DNS-записи — обычно
`A @ → 76.76.21.21` и `CNAME www → cname.vercel-dns.com`, но **сверяйтесь с тем, что показал
именно ваш Vercel**. Прописать их в панели hoster.kz, удалив парковочные записи хостера.
Обновление DNS — от 15 минут до нескольких часов, HTTPS Vercel выпустит сам.

Потом в Supabase → **Authentication** → **URL Configuration** вписать домен в **Site URL**.

### Прибраться перед первыми студентами

- [ ] Удалить тестовые аккаунты: `smoke.test.es@gmail.com`, `recheck.two.es@gmail.com`,
      `aigerim.live.es@gmail.com` — Supabase → **Authentication** → **Users**.
- [ ] Удалить старый проект Supabase в регионе Сидней.
- [ ] **Роль выбирает сам регистрирующийся.** Так в `CLAUDE.md`, так и сделано — но это значит,
      что любой человек по ссылке может завести себе аккаунт преподавателя и увидеть учеников
      своей группы. Убрать выбор роли из формы и выдавать роль преподавателя вручную.
- [ ] Регистрация открыта всем. Позже — код приглашения.
- [ ] Бэкапы базы: на бесплатном плане их нет, нужен Supabase Pro.
- [ ] `teaches(uuid, uuid)` вызывается через REST — зная два UUID, можно узнать, ведёт ли
      преподаватель этого студента. Просто отозвать `EXECUTE` нельзя: RLS-политики выполняются
      от имени вызывающей роли и сломаются. Правильно — вынести функцию из схемы `public`
      и переписать политики. Разобрать при работе над этапом 5.
- [ ] Проект на диске лежит в папке с `#` в имени — Next.js на таком пути не запускается
      локально. Перенести в путь без спецсимволов.
